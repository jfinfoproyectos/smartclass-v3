"use client";

// Push notifications have been completely disabled across the application for all roles
export function PushNotificationToggle(_props: { asMenuItem?: boolean; className?: string }) {
  return null;
}
